const MyObject = {
        js:'javascript',
        cpp:'c++',
        rb:'ruby',
        swift:'swift by apple'
        
};

for (const key in MyObject) {
    const element = MyObject[key];
    console.log(`${key} : ${element}`);
    
    
}

let map = new Map()
map.set('IN','india')
map.set(US,'Uniterd state')
map.set('Fr','france')

for (const key in map) {
    const element = object[key];
    
    
}

let games = {
    game1:'GTA-VC',
    game2:'BGMI',
    game3:'Payback2',
    game2:'miniMillasia',
}